<h3>Edit "{{ name }}"</h3>

<p>Name:<br> {{ input value=name }}</p>

<p>Price (in cents):<br> {{ input value=price }}</p>

<p>Description:<br> {{ textarea value=description rows=8 cols=100}}</p>

<p>Image URL:<br> {{ textarea value=image rows=1 cols=50 }}</p>

<button class="btn btn-info btn-sm" {{action "saveProduct" this}}>Save</button>
<button class="btn btn-danger btn-sm" {{ action "deleteProduct" this }}>Delete</button>